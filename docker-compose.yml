services:
  kith:
    build: .
    ports:
      - "7400:7400"
    volumes:
      - kith-data:/data
    restart: unless-stopped
    environment:
      - PORT=7400
      - DATA_DIR=/data

volumes:
  kith-data:
