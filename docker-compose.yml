services:
  hearth:
    build: .
    ports:
      - "7400:7400"
    volumes:
      - hearth-data:/data
    restart: unless-stopped
    environment:
      - PORT=7400
      - DATA_DIR=/data

volumes:
  hearth-data:
